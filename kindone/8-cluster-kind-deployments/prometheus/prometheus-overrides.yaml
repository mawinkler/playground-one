grafana:
  enabled: true
  service:
    type: LoadBalancer

prometheusOperator:
  enabled: true
  service:
    type: LoadBalancer
  namespaces:
    releaseNamespace: true
    additional:
    - kube-system
    - trendmicro-system
    - attackers
    - goat
    - victims

prometheus:
  enabled: true
  service:
    type: LoadBalancer
